import React, {useState} from 'react'
import {useFood} from '../../context/FoodContext'


const Estadisticas = () => {

    const {getFoods, foods} = useFood()

    useEffect(() => {
        effect
        return () => {
            cleanup
        };
    }, []);

  return (
    <div>

    </div>
  )
}

export default Estadisticas